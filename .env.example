NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Password gate. Leave unset and the site is public.
# Set it (8-200 characters) and every route redirects to /unlock until a
# visitor enters it. Changing the value signs everyone out. Server-only —
# never rename this with a NEXT_PUBLIC_ prefix.
# SITE_PASSWORD=
